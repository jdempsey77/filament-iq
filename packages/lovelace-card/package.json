{
  "name": "filament-iq-manager",
  "version": "1.9.9",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:standalone": "vite --config vite.standalone.config.js",
    "build": "vite build && vite build --config vite.printer.config.js && vite build --config vite.standalone.config.js",
    "build:standalone": "vite build --config vite.standalone.config.js",
    "preview": "vite preview",
    "deploy": "vite build && vite build --config vite.printer.config.js && node scripts/deploy.mjs",
    "smoke": "node scripts/smoke-test.mjs"
  },
  "dependencies": {
    "preact": "^10.19.0"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.8.2",
    "dotenv": "^17.3.1",
    "vite": "^5.0.12"
  }
}
