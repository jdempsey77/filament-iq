# Scripts (included from configuration.yaml).
# Root keys are script IDs (e.g. reload_spoolman_integration); each has alias, sequence, etc.
#
# Reload Spoolman (dashboard button): REQUIRED — replace YOUR_SPOOLMAN_ENTRY_ID below
# with your Spoolman config entry ID. Get it: Settings → Devices & Services → Spoolman
# → ⋮ → Configure; the URL ends with /integration/XXXXXXXX — that hex is entry_id.
# Or in .storage/core.config_entries search for "spoolman" and copy "entry_id".
# Then redeploy: ./scripts/manage_ha.sh --scripts (and restart HA if needed).
#
# yaml-language-server: $schema=
# Reload Spoolman integration only. Do NOT run refresh_ams_spool_list here (causes spinners; dropdown is populated by automation when REST data arrives).

# V1 OPINIONATED AUTO-MAPPING: Choose spool for slot (deterministic, minimal logic)
# Returns spool_id via ams_slot_N_spool_id helper. Called by finish automation for each slot.
p1s_choose_spool_for_slot_v1:
  alias: P1S Choose Spool for Slot (V1 Opinionated)
  description: "Deterministic spool selection: binding > material+lightest > overall-lightest. Persists choice."
  mode: queued
  max: 10
  fields:
    slot:
      description: "Slot number (1-6)"
      example: "1"
    material:
      description: "Material (PLA, PETG, ABS, etc.)"
      example: "PLA"
    start_weight_g:
      description: "Start weight in grams"
      example: "500"
  sequence:
    - variables:
        auto_enabled: "{{ states('input_boolean.p1s_auto_mode_opinionated') == 'on' }}"
        # RESILIENT: Check if binding helper exists before reading
        binding_helper_exists: "{{ states('input_text.p1s_slot_to_spool_binding_json') not in ['unavailable', 'unknown', ''] }}"
        binding_raw: "{{ states('input_text.p1s_slot_to_spool_binding_json') | default('{}', true) | string }}"
        binding_dict: >
          {% set s = (binding_raw | trim) %}
          {% if s.startswith('{') and s.endswith('}') %}
            {{ s | from_json }}
          {% else %}
            {{ {} }}
          {% endif %}
        slot_str: "{{ slot_number | string }}"
        bound_spool_id: "{{ binding_dict.get(slot_str, 0) | int }}"
    # If auto mode OFF: return null (do nothing)
    - if:
        - condition: template
          value_template: "{{ not auto_enabled }}"
      then:
        # RESILIENT: Only write if helper exists
        - if:
            - condition: template
              value_template: "{{ states('input_text.p1s_last_mapping_json') not in ['unavailable', 'unknown', ''] }}"
          then:
            - service: input_text.set_value
              target:
                entity_id: input_text.p1s_last_mapping_json
              data:
                value: "{{ ('slot=' ~ slot_number ~ ' | auto_mode=OFF | no_action') | string }}"
        - stop: "Auto mode disabled"
    # Check if binding exists and spool exists
    - if:
        - condition: template
          value_template: "{{ bound_spool_id > 0 }}"
      then:
        - variables:
            spool_exists: >
              {{ states('sensor.spoolman_spool_' ~ bound_spool_id) not in ['unavailable', 'unknown', ''] }}
        - if:
            - condition: template
              value_template: "{{ spool_exists }}"
          then:
            - service: input_text.set_value
              target:
                entity_id: "input_text.ams_slot_{{ slot_number }}_spool_id"
              data:
                value: "{{ bound_spool_id | string }}"
            # RESILIENT: Only write if helper exists
            - if:
                - condition: template
                  value_template: "{{ states('input_text.p1s_last_mapping_json') not in ['unavailable', 'unknown', ''] }}"
              then:
                - service: input_text.set_value
                  target:
                    entity_id: input_text.p1s_last_mapping_json
                  data:
                    value: "{{ ('slot=' ~ slot_number ~ ' | spool_id=' ~ bound_spool_id ~ ' | confidence=high | rule=binding_reused') | string }}"
            - stop: "Binding reused"
    # Choose by material + lightest, or fallback to overall-lightest
    - variables:
        mat_lower: "{{ (material | default('PLA') | lower | trim) }}"
        all_spools: >
          {% set ns = namespace(spools=[]) %}
          {% for st in (states.sensor | selectattr('entity_id', 'match', '^sensor\\.spoolman_spool_\\d+$') | list) %}
            {% set mat = (state_attr(st.entity_id, 'filament_material') or '') | string | lower %}
            {% set rem = state_attr(st.entity_id, 'remaining_weight') | float(-1) %}
            {% if rem >= 0 %}
              {% set spool_id = st.entity_id.split('_')[-1] %}
              {% set ns.spools = ns.spools + [{'id': spool_id, 'material': mat, 'remaining': rem}] %}
            {% endif %}
          {% endfor %}
          {{ ns.spools }}
        material_matches: "{{ all_spools | selectattr('material', 'eq', mat_lower) | list }}"
        candidates: "{{ material_matches if material_matches | length > 0 else all_spools }}"
        sorted_candidates: "{{ candidates | sort(attribute='remaining') }}"
        chosen: "{{ sorted_candidates[0] if sorted_candidates | length > 0 else none }}"
        chosen_spool_id: "{{ chosen.id if chosen else 0 }}"
        confidence: "{{ 'medium' if material_matches | length > 0 else 'low' }}"
        rule: "{{ 'material_lightest' if material_matches | length > 0 else 'fallback_overall_lightest' }}"
    - if:
        - condition: template
          value_template: "{{ chosen_spool_id | int > 0 }}"
      then:
        # Assign spool to slot
        - service: input_text.set_value
          target:
            entity_id: "input_text.ams_slot_{{ slot_number }}_spool_id"
          data:
            value: "{{ chosen_spool_id | string }}"
        # Update binding JSON (persist for next time) - RESILIENT: Only if helper exists
        - if:
            - condition: template
              value_template: "{{ binding_helper_exists }}"
          then:
            - variables:
                updated_binding: "{{ binding_dict | combine({slot_str: chosen_spool_id | int}) }}"
                # RESILIENT: Ensure JSON string doesn't exceed max (1024)
                binding_json_str: "{{ (updated_binding | tojson)[:1020] | string }}"
            - service: input_text.set_value
              target:
                entity_id: input_text.p1s_slot_to_spool_binding_json
              data:
                value: "{{ binding_json_str }}"
        # Log mapping result - RESILIENT: Only if helper exists
        - if:
            - condition: template
              value_template: "{{ states('input_text.p1s_last_mapping_json') not in ['unavailable', 'unknown', ''] }}"
          then:
            - variables:
                # RESILIENT: Ensure log string doesn't exceed max (2048)
                log_str: "{{ ('slot=' ~ slot_number ~ ' | mat=' ~ mat_lower ~ ' | spool_id=' ~ chosen_spool_id ~ ' | remaining=' ~ chosen.remaining ~ 'g | confidence=' ~ confidence ~ ' | rule=' ~ rule ~ ' | candidates=' ~ candidates | length)[:2044] | string }}"
            - service: input_text.set_value
              target:
                entity_id: input_text.p1s_last_mapping_json
              data:
                value: "{{ log_str }}"
      else:
        # No spools available (should never happen)
        - if:
            - condition: template
              value_template: "{{ states('input_text.p1s_last_mapping_json') not in ['unavailable', 'unknown', ''] }}"
          then:
            - service: input_text.set_value
              target:
                entity_id: input_text.p1s_last_mapping_json
              data:
                value: "{{ ('slot=' ~ slot_number ~ ' | ERROR | no_spools_available') | string }}"

# E2E VALIDATION: Test scenarios for V1 opinionated auto-mapping
test_scenario_1_single_slot:
  alias: "Test Scenario 1: Single Slot Print (35g)"
  description: "Clear state, seed slot 1 (350g→315g), trigger finish, verify decrement"
  sequence:
    # Clear all start/end helpers
    - repeat:
        count: 6
        sequence:
          - service: input_number.set_value
            target:
              entity_id: "input_number.p1s_start_slot_{{ repeat.index }}_g"
            data:
              value: 0
          - service: input_number.set_value
            target:
              entity_id: "input_number.p1s_end_slot_{{ repeat.index }}_g"
            data:
              value: 0
    # Seed slot 1
    - service: input_number.set_value
      target:
        entity_id: input_number.p1s_start_slot_1_g
      data:
        value: 350
    - service: input_number.set_value
      target:
        entity_id: input_number.p1s_end_slot_1_g
      data:
        value: 315
    # Trigger finish
    - service: input_boolean.turn_off
      target:
        entity_id: input_boolean.p1s_debug_finish_trigger
    - delay: "00:00:01"
    - service: input_boolean.turn_on
      target:
        entity_id: input_boolean.p1s_debug_finish_trigger

test_scenario_4_multi_slot:
  alias: "Test Scenario 4: Multi-Slot Print"
  description: "Seed slot 1 (300g→270g) + slot 2 (200g→165g), verify both processed"
  sequence:
    # Clear all
    - repeat:
        count: 6
        sequence:
          - service: input_number.set_value
            target:
              entity_id: "input_number.p1s_start_slot_{{ repeat.index }}_g"
            data:
              value: 0
          - service: input_number.set_value
            target:
              entity_id: "input_number.p1s_end_slot_{{ repeat.index }}_g"
            data:
              value: 0
    # Seed slot 1
    - service: input_number.set_value
      target:
        entity_id: input_number.p1s_start_slot_1_g
      data:
        value: 300
    - service: input_number.set_value
      target:
        entity_id: input_number.p1s_end_slot_1_g
      data:
        value: 270
    # Seed slot 2
    - service: input_number.set_value
      target:
        entity_id: input_number.p1s_start_slot_2_g
      data:
        value: 200
    - service: input_number.set_value
      target:
        entity_id: input_number.p1s_end_slot_2_g
      data:
        value: 165
    # Trigger
    - service: input_boolean.turn_off
      target:
        entity_id: input_boolean.p1s_debug_finish_trigger
    - delay: "00:00:01"
    - service: input_boolean.turn_on
      target:
        entity_id: input_boolean.p1s_debug_finish_trigger

test_clear_binding:
  alias: "Test: Clear Binding JSON"
  description: "Reset binding JSON to {} for determinism tests"
  sequence:
    - service: input_text.set_value
      target:
        entity_id: input_text.p1s_slot_to_spool_binding_json
      data:
        value: "{}"

# INFRASTRUCTURE VALIDATION: Verify persistence helpers exist and are writable
p1s_validate_persistence_infrastructure:
  alias: "P1S Validate Persistence Infrastructure"
  description: "Confirms binding + mapping helpers exist and are writable. Returns PASS/FAIL."
  sequence:
    - variables:
        binding_exists: "{{ states('input_text.p1s_slot_to_spool_binding_json') not in ['unavailable', 'unknown'] }}"
        mapping_exists: "{{ states('input_text.p1s_last_mapping_json') not in ['unavailable', 'unknown'] }}"
        binding_state_before: "{{ states('input_text.p1s_slot_to_spool_binding_json') | default('', true) | string }}"
    # Test 1: Check existence
    - if:
        - condition: template
          value_template: "{{ not binding_exists or not mapping_exists }}"
      then:
        - service: persistent_notification.create
          data:
            title: "P1S Infrastructure Validation: FAIL"
            message: >
              Binding helper exists: {{ binding_exists }}
              Mapping helper exists: {{ mapping_exists }}
              
              ACTION REQUIRED: Create missing helpers via HA UI:
              1. Settings → Devices & Services → Helpers
              2. Create Text helpers with exact entity IDs:
                 - input_text.p1s_slot_to_spool_binding_json (max: 1024)
                 - input_text.p1s_last_mapping_json (max: 2048)
        - stop: "FAIL: Helpers missing"
    # Test 2: Attempt safe write/restore (doesn't alter production data long-term)
    - service: input_text.set_value
      target:
        entity_id: input_text.p1s_slot_to_spool_binding_json
      data:
        value: "{{ ('{}') | string }}"
    - delay: "00:00:01"
    - variables:
        write_test: "{{ states('input_text.p1s_slot_to_spool_binding_json') | string }}"
    # Restore original value (if unavailable/unknown, restore '{}')
    - service: input_text.set_value
      target:
        entity_id: input_text.p1s_slot_to_spool_binding_json
      data:
        value: "{{ (binding_state_before if binding_state_before not in ['', 'unavailable', 'unknown'] else '{}') | string }}"
    # Test 3: Log result
    - service: input_text.set_value
      target:
        entity_id: input_text.p1s_last_mapping_json
      data:
        value: "{{ ('validation=PASS | binding_writable=true | mapping_writable=true | timestamp=' ~ now().isoformat()) | string }}"
    - service: persistent_notification.create
      data:
        title: "P1S Infrastructure Validation: PASS"
        message: >
          ✓ Binding helper exists and is writable
          ✓ Mapping helper exists and is writable
          
          Infrastructure is ready for V1 opinionated auto-mapping.

# DEBUG: Simulate finish path without a real print. Requires filament_debug_mode ON for checkpoints.
# Seeds start/end input_numbers (NO JSON), fires finish automation. Verify checkpoints reach COMPLETE.
# Spoolman decrement only runs if ams_slot_1_spool_id/ams_slot_2_spool_id are set and > 0.
p1s_debug_force_finish_path:
  alias: P1S Debug – force finish path (no print)
  sequence:
    # Set start values
    - service: input_number.set_value
      target:
        entity_id: input_number.p1s_start_slot_1_g
      data:
        value: 100
    - service: input_number.set_value
      target:
        entity_id: input_number.p1s_start_slot_2_g
      data:
        value: 200
    # Set end values
    - service: input_number.set_value
      target:
        entity_id: input_number.p1s_end_slot_1_g
      data:
        value: 90
    - service: input_number.set_value
      target:
        entity_id: input_number.p1s_end_slot_2_g
      data:
        value: 150
    # Trigger finish automation
    - service: input_boolean.turn_off
      target:
        entity_id: input_boolean.p1s_debug_finish_trigger
    - delay: "00:00:01"
    - service: input_boolean.turn_on
      target:
        entity_id: input_boolean.p1s_debug_finish_trigger

reload_spoolman_integration:
  alias: Reload Spoolman integration
  sequence:
    - action: homeassistant.reload_config_entry
      data:
        entry_id: "01KGX5YPH9CXR2Y1KZRVSP48V7"

# Add filament to Spoolman from test dashboard form (uses spoolman_new_filament_* helpers). Then reload integration.
# If spoolman_base_url is empty, request goes to localhost:8080 and the filament won't appear in your Spoolman.
spoolman_add_filament:
  alias: Add filament to Spoolman
  sequence:
    - action: rest_command.ams_spoolman_create_filament
    - action: script.reload_spoolman_integration
    - action: notify.persistent_notification
      data:
        message: "Filament created and Spoolman reloaded."
        title: Filament added to Spoolman

# Add spool to Spoolman from dashboard (uses spoolman_new_spool_* helpers).
spoolman_add_spool:
  alias: Add spool to Spoolman
  sequence:
    - choose:
        - conditions:
            - condition: template
              value_template: "{{ (states('input_select.spoolman_new_spool_filament') or '') | trim != '' and ' - ' in (states('input_select.spoolman_new_spool_filament') or '') }}"
          sequence:
            - variables:
                qty: "{{ states('input_number.spoolman_new_spool_quantity') | int(1) }}"
            - repeat:
                count: "{{ qty }}"
                sequence:
                  - action: rest_command.ams_spoolman_create_spool
            - action: script.reload_spoolman_integration
            - action: browser_mod.notification
              data:
                message: "✅ Spool added to Spoolman"

# Manual entrypoint for AMS reconciliation (fires AMS_RECONCILE_ALL event for AppDaemon).
reconcile_all_ams_slots:
  alias: Reconcile AMS Slots
  description: "Manual entrypoint for AMS reconciliation (fires AMS_RECONCILE_ALL event)"
  sequence:
    - event: AMS_RECONCILE_ALL
      event_data:
        status_only: false
        reason: "manual_button"

# Optional manual refresh: re-apply dropdown from template sensor options only (no Spoolman reload).
refresh_ams_spool_list:
  alias: Refresh AMS spool list (Assign from warehouse)
  sequence:
    - action: input_select.set_options
      target:
        entity_id: input_select.ams_assign_source_spool
      data:
        options: "{{ ['— Select spool —'] + (state_attr('sensor.ams_spool_list_options', 'options') or []) }}"

# Force Spoolman filaments fetch and repopulate "Add spool" filament dropdown. Use if dropdown is empty (wrong URL or sensor not yet updated).
refresh_filament_dropdown:
  alias: Refresh filament list (Add spool)
  sequence:
    - action: homeassistant.update_entity
      target:
        entity_id:
          - sensor.spoolman_filaments_api
          - sensor.spoolman_vendors_api
          - sensor.spoolman_spools_api
    - delay:
        seconds: 2
    - action: input_select.set_options
      target:
        entity_id: input_select.spoolman_new_spool_filament
      data:
        options: "{{ ['— Select filament —'] + (state_attr('sensor.ams_filament_list_options', 'options') or []) }}"

# Assign the currently selected warehouse spool to a slot (sets input_text.ams_slot_N_spool_id).
# No-op if no spool selected (source ID 0).
ams_assign_to_slot_1:
  alias: Assign selected spool to Slot 1
  sequence:
    - choose:
        - conditions:
            - condition: template
              value_template: "{{ states('input_number.ams_assign_source_spool_id') | int > 0 }}"
          sequence:
            - action: input_text.set_value
              target:
                entity_id: input_text.ams_slot_1_spool_id
              data:
                value: "{{ states('input_number.ams_assign_source_spool_id') | int }}"
ams_assign_to_slot_2:
  alias: Assign selected spool to Slot 2
  sequence:
    - choose:
        - conditions:
            - condition: template
              value_template: "{{ states('input_number.ams_assign_source_spool_id') | int > 0 }}"
          sequence:
            - action: input_text.set_value
              target:
                entity_id: input_text.ams_slot_2_spool_id
              data:
                value: "{{ states('input_number.ams_assign_source_spool_id') | int }}"
ams_assign_to_slot_3:
  alias: Assign selected spool to Slot 3
  sequence:
    - choose:
        - conditions:
            - condition: template
              value_template: "{{ states('input_number.ams_assign_source_spool_id') | int > 0 }}"
          sequence:
            - action: input_text.set_value
              target:
                entity_id: input_text.ams_slot_3_spool_id
              data:
                value: "{{ states('input_number.ams_assign_source_spool_id') | int }}"
ams_assign_to_slot_4:
  alias: Assign selected spool to Slot 4
  sequence:
    - choose:
        - conditions:
            - condition: template
              value_template: "{{ states('input_number.ams_assign_source_spool_id') | int > 0 }}"
          sequence:
            - action: input_text.set_value
              target:
                entity_id: input_text.ams_slot_4_spool_id
              data:
                value: "{{ states('input_number.ams_assign_source_spool_id') | int }}"
ams_assign_to_slot_5:
  alias: Assign selected spool to Slot 5 (AMS2 HT 1)
  sequence:
    - choose:
        - conditions:
            - condition: template
              value_template: "{{ states('input_number.ams_assign_source_spool_id') | int > 0 }}"
          sequence:
            - action: input_text.set_value
              target:
                entity_id: input_text.ams_slot_5_spool_id
              data:
                value: "{{ states('input_number.ams_assign_source_spool_id') | int }}"
ams_assign_to_slot_6:
  alias: Assign selected spool to Slot 6 (AMS2 HT 2)
  sequence:
    - choose:
        - conditions:
            - condition: template
              value_template: "{{ states('input_number.ams_assign_source_spool_id') | int > 0 }}"
          sequence:
            - action: input_text.set_value
              target:
                entity_id: input_text.ams_slot_6_spool_id
              data:
                value: "{{ states('input_number.ams_assign_source_spool_id') | int }}"

# -----------------------------------------------------------------------------
# AMS Slot Manager: scale-first update. remaining = max(0, gross - tare).
# Tare from spool type (Bambu Lab / Overture cardboard) or per-slot override (Custom).
# Spool ID from input_text.ams_slot_N_spool_id (persisted, may not be 1-6).
# -----------------------------------------------------------------------------
ams_update_slot_1:
  alias: AMS Slot 1 – update weight (gross − tare → Spoolman)
  sequence:
    - variables:
        spool_id: "{{ states('input_text.ams_slot_1_spool_id') }}"
        gross: "{{ states('input_number.ams_slot_1_gross_weight') | float(0) }}"
        stype: "{{ states('input_select.ams_slot_1_spool_type') }}"
        tare: "{% if stype == 'Bambu Lab (plastic)' %}{{ states('input_number.tare_bambu_lab_plastic') | float(0) }}{% elif stype == 'Overture cardboard' %}{{ states('input_number.tare_overture_cardboard') | float(0) }}{% else %}{{ states('input_number.ams_slot_1_tare_override') | float(0) }}{% endif %}"
        remaining: "{{ [0, gross - tare] | max }}"
    - service: spoolman.patch_spool
      data:
        id: "{{ spool_id | int }}"
        remaining_weight: "{{ remaining }}"
ams_update_slot_2:
  alias: AMS Slot 2 – update weight (gross − tare → Spoolman)
  sequence:
    - variables:
        spool_id: "{{ states('input_text.ams_slot_2_spool_id') }}"
        gross: "{{ states('input_number.ams_slot_2_gross_weight') | float(0) }}"
        stype: "{{ states('input_select.ams_slot_2_spool_type') }}"
        tare: "{% if stype == 'Bambu Lab (plastic)' %}{{ states('input_number.tare_bambu_lab_plastic') | float(0) }}{% elif stype == 'Overture cardboard' %}{{ states('input_number.tare_overture_cardboard') | float(0) }}{% else %}{{ states('input_number.ams_slot_2_tare_override') | float(0) }}{% endif %}"
        remaining: "{{ [0, gross - tare] | max }}"
    - service: spoolman.patch_spool
      data:
        id: "{{ spool_id | int }}"
        remaining_weight: "{{ remaining }}"
ams_update_slot_3:
  alias: AMS Slot 3 – update weight (gross − tare → Spoolman)
  sequence:
    - variables:
        spool_id: "{{ states('input_text.ams_slot_3_spool_id') }}"
        gross: "{{ states('input_number.ams_slot_3_gross_weight') | float(0) }}"
        stype: "{{ states('input_select.ams_slot_3_spool_type') }}"
        tare: "{% if stype == 'Bambu Lab (plastic)' %}{{ states('input_number.tare_bambu_lab_plastic') | float(0) }}{% elif stype == 'Overture cardboard' %}{{ states('input_number.tare_overture_cardboard') | float(0) }}{% else %}{{ states('input_number.ams_slot_3_tare_override') | float(0) }}{% endif %}"
        remaining: "{{ [0, gross - tare] | max }}"
    - service: spoolman.patch_spool
      data:
        id: "{{ spool_id | int }}"
        remaining_weight: "{{ remaining }}"
ams_update_slot_4:
  alias: AMS Slot 4 – update weight (gross − tare → Spoolman)
  sequence:
    - variables:
        spool_id: "{{ states('input_text.ams_slot_4_spool_id') }}"
        gross: "{{ states('input_number.ams_slot_4_gross_weight') | float(0) }}"
        stype: "{{ states('input_select.ams_slot_4_spool_type') }}"
        tare: "{% if stype == 'Bambu Lab (plastic)' %}{{ states('input_number.tare_bambu_lab_plastic') | float(0) }}{% elif stype == 'Overture cardboard' %}{{ states('input_number.tare_overture_cardboard') | float(0) }}{% else %}{{ states('input_number.ams_slot_4_tare_override') | float(0) }}{% endif %}"
        remaining: "{{ [0, gross - tare] | max }}"
    - service: spoolman.patch_spool
      data:
        id: "{{ spool_id | int }}"
        remaining_weight: "{{ remaining }}"
ams_update_slot_5:
  alias: AMS Slot 5 (HT1) – update weight (gross − tare → Spoolman)
  sequence:
    - variables:
        spool_id: "{{ states('input_text.ams_slot_5_spool_id') }}"
        gross: "{{ states('input_number.ams_slot_5_gross_weight') | float(0) }}"
        stype: "{{ states('input_select.ams_slot_5_spool_type') }}"
        tare: "{% if stype == 'Bambu Lab (plastic)' %}{{ states('input_number.tare_bambu_lab_plastic') | float(0) }}{% elif stype == 'Overture cardboard' %}{{ states('input_number.tare_overture_cardboard') | float(0) }}{% else %}{{ states('input_number.ams_slot_5_tare_override') | float(0) }}{% endif %}"
        remaining: "{{ [0, gross - tare] | max }}"
    - service: spoolman.patch_spool
      data:
        id: "{{ spool_id | int }}"
        remaining_weight: "{{ remaining }}"
ams_update_slot_6:
  alias: AMS Slot 6 (HT2) – update weight (gross − tare → Spoolman)
  sequence:
    - variables:
        spool_id: "{{ states('input_text.ams_slot_6_spool_id') }}"
        gross: "{{ states('input_number.ams_slot_6_gross_weight') | float(0) }}"
        stype: "{{ states('input_select.ams_slot_6_spool_type') }}"
        tare: "{% if stype == 'Bambu Lab (plastic)' %}{{ states('input_number.tare_bambu_lab_plastic') | float(0) }}{% elif stype == 'Overture cardboard' %}{{ states('input_number.tare_overture_cardboard') | float(0) }}{% else %}{{ states('input_number.ams_slot_6_tare_override') | float(0) }}{% endif %}"
        remaining: "{{ [0, gross - tare] | max }}"
    - service: spoolman.patch_spool
      data:
        id: "{{ spool_id | int }}"
        remaining_weight: "{{ remaining }}"

# Legacy: direct remaining (g). Prefer ams_update_slot_N with gross + spool type.
update_spoolman_tray_1:
  alias: Update Spoolman tray 1 remaining weight (legacy)
  sequence:
    - service: spoolman.patch_spool
      data:
        id: "{{ states('input_text.ams_slot_1_spool_id') | int }}"
        remaining_weight: "{{ states('input_number.spoolman_tray_1_remaining') | float }}"
update_spoolman_tray_2:
  alias: Update Spoolman tray 2 remaining weight (legacy)
  sequence:
    - service: spoolman.patch_spool
      data:
        id: "{{ states('input_text.ams_slot_2_spool_id') | int }}"
        remaining_weight: "{{ states('input_number.spoolman_tray_2_remaining') | float }}"
update_spoolman_tray_3:
  alias: Update Spoolman tray 3 remaining weight (legacy)
  sequence:
    - service: spoolman.patch_spool
      data:
        id: "{{ states('input_text.ams_slot_3_spool_id') | int }}"
        remaining_weight: "{{ states('input_number.spoolman_tray_3_remaining') | float }}"
update_spoolman_tray_4:
  alias: Update Spoolman tray 4 remaining weight (legacy)
  sequence:
    - service: spoolman.patch_spool
      data:
        id: "{{ states('input_text.ams_slot_4_spool_id') | int }}"
        remaining_weight: "{{ states('input_number.spoolman_tray_4_remaining') | float }}"
update_spoolman_tray_5:
  alias: Update Spoolman tray 5 (HT1) remaining weight (legacy)
  sequence:
    - service: spoolman.patch_spool
      data:
        id: "{{ states('input_text.ams_slot_5_spool_id') | int }}"
        remaining_weight: "{{ states('input_number.spoolman_tray_5_remaining') | float }}"
update_spoolman_tray_6:
  alias: Update Spoolman tray 6 (HT2) remaining weight (legacy)
  sequence:
    - service: spoolman.patch_spool
      data:
        id: "{{ states('input_text.ams_slot_6_spool_id') | int }}"
        remaining_weight: "{{ states('input_number.spoolman_tray_6_remaining') | float }}"

# Combined assign + update scripts (one per slot). Parse dropdown, assign if valid, calculate remaining, update Spoolman.
ams_slot_1_assign_and_update:
  alias: Slot 1 – Assign & Update
  sequence:
    - variables:
        selected: "{{ states('input_select.ams_slot_1_select_spool') }}"
        spool_id: "{{ (selected.split('(')[-1].split(')')[0] | trim | int(0)) if (selected != '— Select spool —' and '(' in selected) else 0 }}"
        old_spool_id: "{{ states('input_text.ams_slot_1_spool_id') | int(0) }}"
    - choose:
        - conditions:
            - condition: template
              value_template: "{{ spool_id > 0 }}"
          sequence:
            - service: input_text.set_value
              target:
                entity_id: input_text.ams_slot_1_spool_id
              data:
                value: "{{ spool_id }}"
    - choose:
        - conditions:
            - condition: template
              value_template: "{{ old_spool_id > 0 and old_spool_id != spool_id }}"
          sequence:
            - service: spoolman.patch_spool
              data:
                id: "{{ old_spool_id }}"
                location: "Shelf"
    - variables:
        final_spool_id: "{{ states('input_text.ams_slot_1_spool_id') | int }}"
    - choose:
        - conditions:
            - condition: template
              value_template: "{{ final_spool_id > 0 }}"
          sequence:
            - service: spoolman.patch_spool
              data:
                id: "{{ final_spool_id }}"
                location: "AMS1_Slot1"
            - service: script.reload_spoolman_integration
            - event: bambu_rfid_manual_enroll_tag_to_spool
              event_data:
                slot: 1
                spool_id: "{{ final_spool_id }}"
            - action: browser_mod.notification
              data:
                message: "✅ Slot 1 updated"
        default:
          - action: browser_mod.notification
            data:
              message: "Select a spool from the dropdown first."

ams_slot_2_assign_and_update:
  alias: Slot 2 – Assign & Update
  sequence:
    - variables:
        selected: "{{ states('input_select.ams_slot_2_select_spool') }}"
        spool_id: "{{ (selected.split('(')[-1].split(')')[0] | trim | int(0)) if (selected != '— Select spool —' and '(' in selected) else 0 }}"
        old_spool_id: "{{ states('input_text.ams_slot_2_spool_id') | int(0) }}"
    - choose:
        - conditions:
            - condition: template
              value_template: "{{ spool_id > 0 }}"
          sequence:
            - service: input_text.set_value
              target:
                entity_id: input_text.ams_slot_2_spool_id
              data:
                value: "{{ spool_id }}"
    - choose:
        - conditions:
            - condition: template
              value_template: "{{ old_spool_id > 0 and old_spool_id != spool_id }}"
          sequence:
            - service: spoolman.patch_spool
              data:
                id: "{{ old_spool_id }}"
                location: "Shelf"
    - variables:
        final_spool_id: "{{ states('input_text.ams_slot_2_spool_id') | int }}"
    - choose:
        - conditions:
            - condition: template
              value_template: "{{ final_spool_id > 0 }}"
          sequence:
            - service: spoolman.patch_spool
              data:
                id: "{{ final_spool_id }}"
                location: "AMS1_Slot2"
            - service: script.reload_spoolman_integration
            - event: bambu_rfid_manual_enroll_tag_to_spool
              event_data:
                slot: 2
                spool_id: "{{ final_spool_id }}"
            - action: browser_mod.notification
              data:
                message: "✅ Slot 2 updated"
        default:
          - action: browser_mod.notification
            data:
              message: "Select a spool from the dropdown first."

ams_slot_3_assign_and_update:
  alias: Slot 3 – Assign & Update
  sequence:
    - variables:
        selected: "{{ states('input_select.ams_slot_3_select_spool') }}"
        spool_id: "{{ (selected.split('(')[-1].split(')')[0] | trim | int(0)) if (selected != '— Select spool —' and '(' in selected) else 0 }}"
        old_spool_id: "{{ states('input_text.ams_slot_3_spool_id') | int(0) }}"
    - choose:
        - conditions:
            - condition: template
              value_template: "{{ spool_id > 0 }}"
          sequence:
            - service: input_text.set_value
              target:
                entity_id: input_text.ams_slot_3_spool_id
              data:
                value: "{{ spool_id }}"
    - choose:
        - conditions:
            - condition: template
              value_template: "{{ old_spool_id > 0 and old_spool_id != spool_id }}"
          sequence:
            - service: spoolman.patch_spool
              data:
                id: "{{ old_spool_id }}"
                location: "Shelf"
    - variables:
        final_spool_id: "{{ states('input_text.ams_slot_3_spool_id') | int }}"
    - choose:
        - conditions:
            - condition: template
              value_template: "{{ final_spool_id > 0 }}"
          sequence:
            - service: spoolman.patch_spool
              data:
                id: "{{ final_spool_id }}"
                location: "AMS1_Slot3"
            - service: script.reload_spoolman_integration
            - event: bambu_rfid_manual_enroll_tag_to_spool
              event_data:
                slot: 3
                spool_id: "{{ final_spool_id }}"
            - action: browser_mod.notification
              data:
                message: "✅ Slot 3 updated"
        default:
          - action: browser_mod.notification
            data:
              message: "Select a spool from the dropdown first."

ams_slot_4_assign_and_update:
  alias: Slot 4 – Assign & Update
  sequence:
    - variables:
        selected: "{{ states('input_select.ams_slot_4_select_spool') }}"
        spool_id: "{{ (selected.split('(')[-1].split(')')[0] | trim | int(0)) if (selected != '— Select spool —' and '(' in selected) else 0 }}"
        old_spool_id: "{{ states('input_text.ams_slot_4_spool_id') | int(0) }}"
    - choose:
        - conditions:
            - condition: template
              value_template: "{{ spool_id > 0 }}"
          sequence:
            - service: input_text.set_value
              target:
                entity_id: input_text.ams_slot_4_spool_id
              data:
                value: "{{ spool_id }}"
    - choose:
        - conditions:
            - condition: template
              value_template: "{{ old_spool_id > 0 and old_spool_id != spool_id }}"
          sequence:
            - service: spoolman.patch_spool
              data:
                id: "{{ old_spool_id }}"
                location: "Shelf"
    - variables:
        final_spool_id: "{{ states('input_text.ams_slot_4_spool_id') | int }}"
    - choose:
        - conditions:
            - condition: template
              value_template: "{{ final_spool_id > 0 }}"
          sequence:
            - service: spoolman.patch_spool
              data:
                id: "{{ final_spool_id }}"
                location: "AMS1_Slot4"
            - service: script.reload_spoolman_integration
            - event: bambu_rfid_manual_enroll_tag_to_spool
              event_data:
                slot: 4
                spool_id: "{{ final_spool_id }}"
            - action: browser_mod.notification
              data:
                message: "✅ Slot 4 updated"
        default:
          - action: browser_mod.notification
            data:
              message: "Select a spool from the dropdown first."

ams_slot_5_assign_and_update:
  alias: Slot 5 – Assign & Update
  sequence:
    - variables:
        selected: "{{ states('input_select.ams_slot_5_select_spool') }}"
        spool_id: "{{ (selected.split('(')[-1].split(')')[0] | trim | int(0)) if (selected != '— Select spool —' and '(' in selected) else 0 }}"
        old_spool_id: "{{ states('input_text.ams_slot_5_spool_id') | int(0) }}"
    - choose:
        - conditions:
            - condition: template
              value_template: "{{ spool_id > 0 }}"
          sequence:
            - service: input_text.set_value
              target:
                entity_id: input_text.ams_slot_5_spool_id
              data:
                value: "{{ spool_id }}"
    - choose:
        - conditions:
            - condition: template
              value_template: "{{ old_spool_id > 0 and old_spool_id != spool_id }}"
          sequence:
            - service: spoolman.patch_spool
              data:
                id: "{{ old_spool_id }}"
                location: "Shelf"
    - variables:
        final_spool_id: "{{ states('input_text.ams_slot_5_spool_id') | int }}"
    - choose:
        - conditions:
            - condition: template
              value_template: "{{ final_spool_id > 0 }}"
          sequence:
            - service: spoolman.patch_spool
              data:
                id: "{{ final_spool_id }}"
                location: "AMS2_HT_Slot1"
            - service: script.reload_spoolman_integration
            - event: bambu_rfid_manual_enroll_tag_to_spool
              event_data:
                slot: 5
                spool_id: "{{ final_spool_id }}"
            - action: browser_mod.notification
              data:
                message: "✅ Slot 5 updated"
        default:
          - action: browser_mod.notification
            data:
              message: "Select a spool from the dropdown first."

ams_slot_6_assign_and_update:
  alias: Slot 6 – Assign & Update
  sequence:
    - variables:
        selected: "{{ states('input_select.ams_slot_6_select_spool') }}"
        spool_id: "{{ (selected.split('(')[-1].split(')')[0] | trim | int(0)) if (selected != '— Select spool —' and '(' in selected) else 0 }}"
        old_spool_id: "{{ states('input_text.ams_slot_6_spool_id') | int(0) }}"
    - choose:
        - conditions:
            - condition: template
              value_template: "{{ spool_id > 0 }}"
          sequence:
            - service: input_text.set_value
              target:
                entity_id: input_text.ams_slot_6_spool_id
              data:
                value: "{{ spool_id }}"
    - choose:
        - conditions:
            - condition: template
              value_template: "{{ old_spool_id > 0 and old_spool_id != spool_id }}"
          sequence:
            - service: spoolman.patch_spool
              data:
                id: "{{ old_spool_id }}"
                location: "Shelf"
    - variables:
        final_spool_id: "{{ states('input_text.ams_slot_6_spool_id') | int }}"
    - choose:
        - conditions:
            - condition: template
              value_template: "{{ final_spool_id > 0 }}"
          sequence:
            - service: spoolman.patch_spool
              data:
                id: "{{ final_spool_id }}"
                location: "AMS2_HT_Slot2"
            - service: script.reload_spoolman_integration
            - event: bambu_rfid_manual_enroll_tag_to_spool
              event_data:
                slot: 6
                spool_id: "{{ final_spool_id }}"
            - action: browser_mod.notification
              data:
                message: "✅ Slot 6 updated"
        default:
          - action: browser_mod.notification
            data:
              message: "Select a spool from the dropdown first."
